# Repository Instructions

All project commands must be run through `uv`.

Use:

```sh
uv run python main.py
uv run pytest
uv run python -m src
```

Do not run `python`, `pip`, or `pytest` directly in this repository.
